page.work
  {% assign items = site.work | sort: 'date' | reverse  %}
  {% for item in items %}
  a(href="{{ site.baseurl }}{{item.url}}" style="background-image: url({{ site.baseurl }}{{item.image_path}})").work-thumb
    .work-meta
      .name {{item.title}}
      .date {{item.date | date: " %b , %Y"}}
  {% endfor %}
