page.work
  .flex.flex--center
    
      
    
      .work-wrap
        {% for work in site.work  %}
        a(href="{{work.url}}" style="background-image: url({{work.image_path}}").work-thumb
          .work-meta
            .name {{work.title}}
            
          {% endfor %}
          
